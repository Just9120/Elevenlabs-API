name: Studio Legacy CD Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy automatic Studio CD is retired
        run: |
          echo "Legacy automatic Studio CD is retired."
          echo "Use Studio Platform Web CD or Studio Platform API CD for isolated platform deployments."
          exit 1
